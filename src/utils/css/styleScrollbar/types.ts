export type StyleScrollbarOptions = {
  /**
   * スクロールバーサムの色
   */
  thumbColor?: string;

  /**
   * スクロールバートラックの色
   */
  trackColor?: string;

  /**
   * domにホバーした際のスクロールバーサムの色
   */
  hoverThumbColor?: string;

  /**
   * domにホバーした際のスクロールバートラックの色
   */
  hoverTrackColor?: string;
};
